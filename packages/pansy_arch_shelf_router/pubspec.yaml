name: pansy_arch_shelf_router

publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=2.19.1 <3.0.0"

dependencies:
  pansy_arch: ^1.0.0
  shelf_router: ^1.1.3

dev_dependencies:
  build_runner: ^2.3.3
  dino_generator: ^1.0.0-pre.4
